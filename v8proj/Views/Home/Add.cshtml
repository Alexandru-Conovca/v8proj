<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Загрузка концепта</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-center mb-6">Загрузка концепт-арта</h1>

        <!-- Форма загрузки -->
        <div class="max-w-lg mx-auto bg-white p-6 rounded-lg shadow-md">
            <form action="/Concept/Upload" method="post" enctype="multipart/form-data" class="space-y-4">
                <!-- Название концепта -->
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-700">Название концепта</label>
                    <input type="text" id="title" name="title" required class="w-full mt-1 p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>

                <!-- Описание концепта -->
                <div>
                    <label for="description" class="block text-sm font-medium text-gray-700">Описание</label>
                    <textarea id="description" name="description" rows="3" required class="w-full mt-1 p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
                </div>

                <!-- Загрузка изображения -->
                <div>
                    <label for="file" class="block text-sm font-medium text-gray-700">Выберите изображение</label>
                    <input type="file" id="file" name="file" accept="image/*" required class="w-full mt-1 p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>

                <!-- Кнопка загрузки -->
                <button type="submit" class="w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700">Загрузить</button>
            </form>
        </div>
    </div>
</body>
</html>
